<tbody>
<tr>
  <td><%= link_to truncate(gist.question.title, length: 25),
                  admin_question_path(gist.question) %></td>
  <td><%= link_to gist_hash(gist.ur), gist.url,
                  target: "_blank" %></td>
  <td><%= gist.user.email %></td>
</tr>
</tbody>
